# DEPRECATED — não use este arquivo para o FastAPI completo.

O Vercel **não** hospeda pollers/watchdog/Postgres.

Use:
- Portal do cliente: pasta `portal/` + `docs/DEPLOY_VERCEL_PORTAL.md`
- Coleta 24/7: `docs/SETUP_EXE_CCA_F.md` (exe no F: da CCA)

O `vercel.json` na raiz agora publica só o `portal/` (estático).
